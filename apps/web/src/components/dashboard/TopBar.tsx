'use client';\n\nimport { motion } from 'framer-motion';\n\ninterface TopBarProps {\n  balance: number;\n  xp: number;\n  user: any;\n}\n\nexport function TopBar({ balance, xp, user }: TopBarProps) {\n  return (\n    <motion.div\n      className=\"glass border-b border-nova-blue/20 px-4 py-3 sticky top-0 z-40\"\n      initial={{ y: -100 }}\n      animate={{ y: 0 }}\n    >\n      <div className=\"flex justify-between items-center\">\n        <div>\n          <h1 className=\"text-xl font-bold gradient-nova bg-clip-text text-transparent\">PROJECT NOVA</h1>\n          <p className=\"text-xs text-nova-gray\">Welcome, {user?.first_name}</p>\n        </div>\n        <div className=\"flex gap-4 text-sm\">\n          <div className=\"text-center\">\n            <div className=\"font-bold text-nova-electric\">{balance.toFixed(2)}</div>\n            <div className=\"text-nova-gray text-xs\">NOVA</div>\n          </div>\n          <div className=\"text-center\">\n            <div className=\"font-bold text-nova-blue\">{xp}</div>\n            <div className=\"text-nova-gray text-xs\">XP</div>\n          </div>\n        </div>\n      </div>\n    </motion.div>\n  );\n}